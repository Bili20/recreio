# Pedra, Papel, Tesoura (Jokenpô) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Implementar o jogo Pedra-Papel-Tesoura contra a máquina (melhor de 5 = primeiro a 3, empate repete), com animação de reveal e integração de recorde, fiel a `design/jokenpo.png`.

**Architecture:** Lógica pura testável em `games/jokenpo/logic.ts`; UI em `games/jokenpo/Jokenpo.tsx` reusando os componentes/tokens/hooks da Fase 1. Recorde = maior sequência de séries vencidas (`submit('jokenpo', n)`).

**Tech Stack:** React 19, TS, Vite, Tailwind v4, Vitest. Já configurados.

**Convenções:** pt-BR; estilo do projeto (aspas simples, sem ponto-e-vírgula); `verbatimModuleSyntax` (use `import type`); `noUnusedLocals`/`noUnusedParameters`. Commits terminam com linha em branco + `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

---

## Task 1: Lógica do Jokenpô (TDD)

**Files:**
- Create: `src/games/jokenpo/logic.ts`
- Create: `src/games/jokenpo/logic.test.ts`

- [ ] **Step 1: Teste que falha** — `src/games/jokenpo/logic.test.ts`:
```ts
import { describe, it, expect } from 'vitest'
import { resultado, frase, jogadaCpu, serieEncerrada, ALVO, JOGADAS } from './logic'

describe('resultado', () => {
  it('pedra vence tesoura', () => expect(resultado('pedra', 'tesoura')).toBe('vitoria'))
  it('tesoura vence papel', () => expect(resultado('tesoura', 'papel')).toBe('vitoria'))
  it('papel vence pedra', () => expect(resultado('papel', 'pedra')).toBe('vitoria'))
  it('perde no caso inverso', () => expect(resultado('tesoura', 'pedra')).toBe('derrota'))
  it('empata com jogadas iguais', () => expect(resultado('papel', 'papel')).toBe('empate'))
})

describe('frase', () => {
  it('descreve a vitória do jogador', () => expect(frase('papel', 'pedra')).toBe('Papel cobre pedra'))
  it('descreve quando a CPU vence', () => expect(frase('papel', 'tesoura')).toBe('Tesoura corta papel'))
  it('texto de empate', () => expect(frase('pedra', 'pedra')).toMatch(/empate/i))
})

describe('jogadaCpu', () => {
  it('usa o rng injetado de forma determinística', () => {
    expect(jogadaCpu(() => 0)).toBe(JOGADAS[0])
    expect(jogadaCpu(() => 0.99)).toBe(JOGADAS[2])
  })
})

describe('serieEncerrada', () => {
  it(`encerra quando alguém chega a ${ALVO}`, () => {
    expect(serieEncerrada(3, 1)).toBe(true)
    expect(serieEncerrada(2, 2)).toBe(false)
  })
})
```

- [ ] **Step 2: Rodar e ver falhar** — `npm run test -- src/games/jokenpo/logic.test.ts` → FAIL (módulo não existe).

- [ ] **Step 3: Implementar `src/games/jokenpo/logic.ts`:**
```ts
// Lógica pura do Pedra-Papel-Tesoura — sem React, testável.

export type Jogada = 'pedra' | 'papel' | 'tesoura'
export type ResultadoRodada = 'vitoria' | 'derrota' | 'empate'

export const JOGADAS: Jogada[] = ['pedra', 'papel', 'tesoura']
export const ALVO = 3 // primeiro a 3 vitórias vence a série (melhor de 5)

// O que cada jogada vence, e o verbo da frase.
const VENCE: Record<Jogada, Jogada> = { pedra: 'tesoura', tesoura: 'papel', papel: 'pedra' }
const VERBO: Record<Jogada, string> = { pedra: 'quebra', tesoura: 'corta', papel: 'cobre' }

export function resultado(jogador: Jogada, cpu: Jogada): ResultadoRodada {
  if (jogador === cpu) return 'empate'
  return VENCE[jogador] === cpu ? 'vitoria' : 'derrota'
}

const cap = (s: string) => s.charAt(0).toUpperCase() + s.slice(1)

// Frase do ponto de vista do vencedor: "Papel cobre pedra".
export function frase(jogador: Jogada, cpu: Jogada): string {
  if (jogador === cpu) return 'Empate — joguem de novo'
  const venc: Jogada = VENCE[jogador] === cpu ? jogador : cpu
  const perd = VENCE[venc]
  return `${cap(venc)} ${VERBO[venc]} ${perd}`
}

// rng injetável para testes determinísticos; min(2,...) protege contra rng() === 1.
export const jogadaCpu = (rng: () => number = Math.random): Jogada =>
  JOGADAS[Math.min(2, Math.floor(rng() * 3))]

export const serieEncerrada = (voce: number, cpu: number): boolean =>
  voce >= ALVO || cpu >= ALVO
```

- [ ] **Step 4: Rodar e ver passar** — `npm run test -- src/games/jokenpo/logic.test.ts` → PASS. Rodar `npm run test` (suíte toda verde).

- [ ] **Step 5: Commit**
```
git add src/games/jokenpo/logic.ts src/games/jokenpo/logic.test.ts
git commit -m "feat: logica do jokenpo (regras, frase, melhor de 5) (TDD)"
```

---

## Task 2: UI do Jokenpô + ícones + ativação + rota

**Files:**
- Modify: `src/components/icons.tsx` (ícones Pedra/Papel/Tesoura)
- Modify: `src/index.css` (append classes RPS, layout 2 colunas, keyframes)
- Create: `src/games/jokenpo/Jokenpo.tsx`
- Modify: `src/games/games.ts` (`jokenpo.ativo = true`)
- Modify: `src/App.tsx` (rota `/jokenpo`)
- Modify: `README.md` (mover Jokenpô para implementado)

- [ ] **Step 1: Adicionar ícones ao fim de `src/components/icons.tsx`** (antes/depois dos demais exports, mantendo o estilo; usa o helper `base` já existente no arquivo):
```tsx
export const IconPedra = (p: IconProps) => (
  <svg {...base(p)}><circle cx="12" cy="12" r="7" /></svg>
)
export const IconPapel = (p: IconProps) => (
  <svg {...base(p)}><rect x="5" y="5" width="14" height="14" rx="3" /></svg>
)
export const IconTesoura = (p: IconProps) => (
  <svg {...base(p)}><path d="M6 6l12 12M18 6 6 18" /></svg>
)
```
> `IconProps` e `base` já estão definidos no topo de `icons.tsx`. Não redefina.

- [ ] **Step 2: APPEND ao fim de `src/index.css`:**
```css
/* ============ Layout de jogo: 2 colunas (jokenpô) ============ */
.game-layout.cols-2 { grid-template-columns: 280px 1fr; }
@media (max-width: 768px) { .game-layout.cols-2 { grid-template-columns: 1fr; } }

/* ============ Pedra-Papel-Tesoura ============ */
.bo5 { display: flex; gap: 8px; margin-top: 6px; }
.bo5 .pip { width: 12px; height: 12px; border-radius: 50%; background: transparent; border: 2px solid var(--border); }
.bo5 .pip.win { background: var(--accent); border-color: var(--accent); }
.bo5 .pip.loss { background: var(--muted); border-color: var(--muted); }

.rps-arena { display: grid; grid-template-columns: 1fr auto 1fr; align-items: center; gap: 16px; width: min(440px, 92%); }
.rps-side { display: flex; flex-direction: column; align-items: center; gap: 10px; }
.rps-label { font-family: var(--font-display); font-size: var(--t-xs); text-transform: uppercase; letter-spacing: 0.12em; color: var(--faint); }
.rps-token {
  width: clamp(96px, 22vw, 132px); aspect-ratio: 1; border-radius: var(--radius-lg);
  display: grid; place-items: center; background: var(--surface-2); border: 1px solid var(--border); color: var(--muted);
  transition: background .2s ease, border-color .2s ease, color .2s ease;
}
.rps-token.win { background: var(--accent-soft); border-color: var(--accent); color: var(--accent); }
.rps-token svg { width: 44%; height: 44%; }
.rps-vs { font-family: var(--font-display); font-weight: 700; color: var(--faint); }

.rps-reveal { animation: rps-reveal .32s ease; }
@keyframes rps-reveal {
  0% { transform: translateY(-6px) scale(0.9); }
  60% { transform: translateY(0) scale(1.04); }
  100% { transform: scale(1); }
}
@media (prefers-reduced-motion: reduce) { .rps-reveal { animation: none; } }

.rps-choices { display: flex; gap: 12px; flex-wrap: wrap; justify-content: center; }
.rps-choice {
  display: flex; flex-direction: column; align-items: center; gap: 6px;
  width: 84px; padding: 14px 8px; border-radius: var(--radius-lg);
  background: var(--surface); border: 1px solid var(--border); color: var(--text); cursor: pointer;
  font-family: var(--font-display); font-size: var(--t-xs);
  transition: border-color .15s ease, background .15s ease, transform .1s ease;
}
.rps-choice:hover:not(:disabled) { border-color: var(--border-strong); }
.rps-choice:active:not(:disabled) { transform: scale(0.97); }
.rps-choice:disabled { opacity: 0.55; cursor: default; }
.rps-choice.active { background: var(--accent-soft); border-color: var(--accent); color: var(--accent-strong); }
.rps-choice svg { width: 26px; height: 26px; }
```

- [ ] **Step 3: Criar `src/games/jokenpo/Jokenpo.tsx`:**
```tsx
import { useEffect, useState } from 'react'
import Panel, { StatRow, Divider } from '../../components/Panel'
import BoardArea from '../../components/BoardArea'
import Button from '../../components/Button'
import { useRecords } from '../../hooks/useRecords'
import { hasRecord } from '../../utils/records'
import { IconPedra, IconPapel, IconTesoura } from '../../components/icons'
import {
  resultado, frase, jogadaCpu, serieEncerrada, ALVO, JOGADAS,
  type Jogada, type ResultadoRodada,
} from './logic'

// Ícone geométrico de cada jogada (mesma linguagem do design).
function iconeDe(j: Jogada) {
  return j === 'pedra' ? <IconPedra /> : j === 'papel' ? <IconPapel /> : <IconTesoura />
}
const ROTULO: Record<Jogada, string> = { pedra: 'Pedra', papel: 'Papel', tesoura: 'Tesoura' }

export default function Jokenpo() {
  const { records, submit, addPlay } = useRecords()
  const [escolha, setEscolha] = useState<Jogada | null>(null)
  const [cpu, setCpu] = useState<Jogada | null>(null)
  const [ultimo, setUltimo] = useState<ResultadoRodada | null>(null)
  const [placar, setPlacar] = useState({ voce: 0, cpu: 0 })
  const [historico, setHistorico] = useState<('vitoria' | 'derrota')[]>([])
  const [streak, setStreak] = useState(0)
  const [revealKey, setRevealKey] = useState(0)
  const [serieRegistrada, setSerieRegistrada] = useState(false)

  const fim = serieEncerrada(placar.voce, placar.cpu)
  const venceuSerie = placar.voce >= ALVO

  // Registra o resultado da série uma única vez (recorde de sequência de séries vencidas).
  useEffect(() => {
    if (!fim || serieRegistrada) return
    setSerieRegistrada(true)
    if (venceuSerie) {
      const nova = streak + 1
      setStreak(nova)
      submit('jokenpo', nova)
    } else {
      setStreak(0)
      addPlay('jokenpo')
    }
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [fim])

  function jogar(j: Jogada) {
    if (fim) return
    const c = jogadaCpu()
    const r = resultado(j, c)
    setEscolha(j); setCpu(c); setUltimo(r); setRevealKey((k) => k + 1)
    if (r === 'vitoria') {
      setPlacar((p) => ({ ...p, voce: p.voce + 1 }))
      setHistorico((h) => [...h, 'vitoria'])
    } else if (r === 'derrota') {
      setPlacar((p) => ({ ...p, cpu: p.cpu + 1 }))
      setHistorico((h) => [...h, 'derrota'])
    }
    // empate: repete a rodada (não altera placar nem histórico)
  }

  function novaSerie() {
    setPlacar({ voce: 0, cpu: 0 }); setHistorico([])
    setEscolha(null); setCpu(null); setUltimo(null); setSerieRegistrada(false)
  }

  // Texto de status acima da arena.
  let status: string
  if (fim) status = venceuSerie ? 'Você venceu a série! 🎉' : 'A CPU venceu a série.'
  else if (ultimo === null) status = 'Escolha sua jogada'
  else if (ultimo === 'empate') status = 'Empate — joguem de novo'
  else status = `${ultimo === 'vitoria' ? 'Você venceu a rodada' : 'A CPU venceu a rodada'} — ${frase(escolha!, cpu!).toLowerCase()}`

  const recorde = hasRecord(records.jokenpo) ? (records.jokenpo!.value as number) : '—'

  // Classe do token: destaca o vencedor da última rodada decidida.
  const tokenCls = (lado: 'voce' | 'cpu') => {
    if (ultimo === null || ultimo === 'empate') return 'rps-token'
    const win = (lado === 'voce' && ultimo === 'vitoria') || (lado === 'cpu' && ultimo === 'derrota')
    return `rps-token${win ? ' win' : ''}`
  }

  return (
    <div className="game-layout cols-2">
      <div className="panel-side-left">
        <Panel title="Melhor de 5">
          <StatRow k="Você" v={placar.voce} />
          <StatRow k="CPU" v={placar.cpu} />
          <div className="bo5" aria-label="Rodadas da série">
            {Array.from({ length: 5 }).map((_, i) => {
              const h = historico[i]
              return <span key={i} className={`pip${h === 'vitoria' ? ' win' : h === 'derrota' ? ' loss' : ''}`} />
            })}
          </div>
          <Divider />
          <StatRow k="Sequência atual" v={streak} />
          <StatRow k="Recorde" v={recorde} />
        </Panel>
      </div>

      <BoardArea status={status}>
        <div className="rps-arena">
          <div className="rps-side">
            <span className="rps-label">Você</span>
            <div key={`v${revealKey}`} className={`${tokenCls('voce')} rps-reveal`}>
              {escolha ? iconeDe(escolha) : null}
            </div>
          </div>
          <span className="rps-vs">VS</span>
          <div className="rps-side">
            <span className="rps-label">CPU</span>
            <div key={`c${revealKey}`} className={`${tokenCls('cpu')} rps-reveal`}>
              {cpu ? iconeDe(cpu) : null}
            </div>
          </div>
        </div>

        {fim ? (
          <Button variant="primary" onClick={novaSerie}>Nova série</Button>
        ) : (
          <div className="rps-choices">
            {JOGADAS.map((j) => (
              <button
                key={j}
                type="button"
                className={`rps-choice${escolha === j ? ' active' : ''}`}
                onClick={() => jogar(j)}
              >
                {iconeDe(j)}
                {ROTULO[j]}
              </button>
            ))}
          </div>
        )}
      </BoardArea>
    </div>
  )
}
```

- [ ] **Step 4: Ativar o jogo em `src/games/games.ts`** — mudar `ativo: false` para `ativo: true` APENAS na entrada `jokenpo` (manter as outras como estão):
```ts
  { id: 'jokenpo', nome: 'Pedra, Papel, Tesoura', rota: '/jokenpo', iconKey: 'jokenpo', ativo: true,
    descricao: 'Melhor de cinco contra o computador. Pura sorte e instinto.' },
```

- [ ] **Step 5: Adicionar a rota em `src/App.tsx`** — importar e registrar `/jokenpo` (manter `/`, `/placar`, `/velha`):
```tsx
import Jokenpo from './games/jokenpo/Jokenpo'
// ...
        <Route path="/jokenpo" element={<Jokenpo />} />
```

- [ ] **Step 6: Atualizar `README.md`** — mover "Pedra-Papel-Tesoura" da lista "Em breve" para a lista de jogos implementados, com uma linha:
```
- **Pedra, Papel, Tesoura** — melhor de cinco (primeiro a 3) contra o computador, com placar e recorde de séries seguidas.
```
E remover "Pedra-Papel-Tesoura" da frase "Em breve (Fase 2): ...".

- [ ] **Step 7: Verificar** — `npx tsc -b` (sem erros), `npm run build` (PASS), `npm run test` (tudo verde). Não rodar `npm run dev` em foreground.

- [ ] **Step 8: Commit**
```
git add src/components/icons.tsx src/index.css src/games/jokenpo/Jokenpo.tsx src/games/games.ts src/App.tsx README.md
git commit -m "feat: jogo pedra-papel-tesoura (melhor de 5, animacao, recorde)"
```

---

## Self-Review (preenchido)
- **Regras / melhor de 5 / empate repete** → Task 1 (lógica + testes). ✓
- **Layout fiel ao mockup (painel bo5, arena, escolhas, vencedor destacado)** → Task 2 CSS + componente. ✓
- **Ícones geométricos pedra/papel/tesoura** → Task 2 Step 1. ✓
- **Animação de reveal respeitando reduced-motion** → keyframes `rps-reveal`. ✓
- **Recorde (sequência de séries vencidas) grava e exibe no Hub/Placar** → componente usa `submit`/`addPlay`/`hasRecord`. ✓
- **Ativar card e rota** → games.ts + App.tsx. ✓
- **pt-BR, estilo do projeto, tipos estritos** → ao longo. ✓
- **Placeholders:** nenhum. **Tipos:** `Jogada`/`ResultadoRodada` consistentes entre logic e componente.
